# Migration Placeholder

This directory is reserved for Alembic or SQL migration scripts.

Recommended next step:
- Initialize Alembic and convert `infra/postgres/init/01_init.sql` into versioned migrations.
